# node-js-skeleton

Skeleton of a project node for a quick start of an API development

After npm install, run "grunt deploy:local" for init project

@todo
1) add db
2) add session
3) add templating engine